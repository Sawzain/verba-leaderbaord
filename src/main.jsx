import React from "react";
import ReactDOM from "react-dom/client";
import VerbaLeaderboard from "./VerbaLeaderboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VerbaLeaderboard />
  </React.StrictMode>
);
